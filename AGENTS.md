# Repository instructions

Read `CLAUDE.md` and `docs/ASANA-HOUSEKEEPING.md` before repository development.
The same bounded, free-only tracking contract applies across coding assistants.
The user requests work; the coordinator handles verified Asana housekeeping
during that authorized session. Status-only requests remain read-only.

Run the checks described in the README for changed code or manifests. Do not
claim an unattended dispatcher, enforced runner budget, or installed skill.
